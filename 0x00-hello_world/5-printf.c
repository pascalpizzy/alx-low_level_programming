#include <stdio.h>

/**
 * main - The main is the entry point of the file
 *
 * Return: the program will return 0 if successfuly executed
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
